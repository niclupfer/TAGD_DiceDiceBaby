using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class James_Enum : MonoBehaviour
{
    public enum manaType { Red, Green, Blue, Crit, Miss, White, Black };
    public enum diceRarity { Epic, Rare, Uncommon, Common };
}
