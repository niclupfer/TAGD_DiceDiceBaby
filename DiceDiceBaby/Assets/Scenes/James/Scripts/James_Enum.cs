using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class James_Enum : MonoBehaviour
{
    public enum manaType { Blue, Red, Green, Crit, Miss, White, Black };
    public enum diceRarity { Epic, Rare, Uncommon, Common };
    public enum damageType { direct, poison, healing };
}
